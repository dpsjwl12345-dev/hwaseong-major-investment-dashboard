{
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build:client",
  "outputDirectory": "dist/public",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
